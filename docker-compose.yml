services:
  mongodb:
    image: mongo:7.0
    container_name: bookreviews-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: bookreviews

volumes:
  mongodb_data: